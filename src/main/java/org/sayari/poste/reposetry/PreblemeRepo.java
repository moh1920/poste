package org.sayari.poste.reposetry;

import org.sayari.poste.entity.Probleme;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PreblemeRepo extends JpaRepository<Probleme,Integer> {
}
